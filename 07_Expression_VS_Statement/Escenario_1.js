// Paso 1: Definir las variables
var radio = 3; // Cambia el valor del radio según sea necesario
var altura = 10; // Cambia el valor de la altura según sea necesario

// Paso 2: Calcular el área de la base
var areaBase = Math.PI * Math.pow(radio, 2);

// Paso 3: Calcular el volumen del cilindro
var volumen = areaBase * altura;

// Paso 4: Mostrar el resultado en la consola
console.log("El volumen del cilindro es: " + volumen);
