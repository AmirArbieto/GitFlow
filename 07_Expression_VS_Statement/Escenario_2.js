// Paso 1: Definir la variable
var numero = 7; // Cambia el valor del número según sea necesario

// Paso 2: Verificar si es par o impar
if (numero % 2 === 0) {
  // Paso 3: Si es par, mostrar un mensaje
  console.log("El número " + numero + " es par.");
} else {
  // Paso 4: Si es impar, mostrar otro mensaje
  console.log("El número " + numero + " es impar.");
}
