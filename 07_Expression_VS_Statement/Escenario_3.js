// Paso 1: Definir la lista de compras
var listaDeCompras = ["leche", "pan", "huevos", "frutas", "detergente"];

// Paso 2: Recorrer la lista de compras con un bucle for
console.log("Tienes que comprar:");
for (var i = 0; i < listaDeCompras.length; i++) {
  // Paso 3: Mostrar cada artículo junto con su posición
  console.log("   - " + listaDeCompras[i]);
}
